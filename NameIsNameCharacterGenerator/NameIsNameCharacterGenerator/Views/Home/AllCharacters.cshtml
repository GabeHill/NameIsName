@using NameIsNameCharacterGenerator.Services
@using NameIsNameCharacterGenerator.Models
@model CharacterList
@{
    int i = 1;

    ViewBag.Title = "All Characters";
}


<h2>AllCharacters</h2>

@*@foreach (var character in Model.Characters)
{

<div class="card-deck">
    <div class="card" style="width:400px">*@
        @*<img class="card-img-top" src="@character.Image" alt="@character.Name">*@
        @*<div class="card-body d-flex flex-column">
            <h4 class="card-title">
                @character.Name
                <span style="float:right">@character.Class</span>
            </h4>
            <p class="card-text">*@@*@character.Description*@@*</p>
            <br />
            @Html.ActionLink("More Info", "Character", new { charID = character.CharcterID }, new {*@ @*@class = "btn btn-primary btn-block mt-auto"*@@*})
        </div>
    </div>
    @if (i == 4)
    {
    i = 0;
        @:</div>
        @:<br />
        @:<div class="card-deck">
        }
    </div>
    }*@

@foreach(Character c in Model.Characters)
{
    <div class="characterHead">
        <h4><a href="~/Home/Character/@c.CharcterID">Name: @c.Name</a></h4>
        <h5>Race: @c.Race</h5>
        <h5>Class: @c.Class</h5>
        <h5>Alignment: @c.Alignment</h5>
        <h5>Background: </h5>
    </div>
    <div class="characterBody">
        <div class="abilityScores">
            <span>Strength: @c.Str</span>
            <span>Dexterity: @c.Dex</span>
            <span>Constitution: @c.Con</span>
            <span>Intelligence: @c.Int</span>
            <span>Wisdom: @c.Wis</span>
            <span>Charisma: @c.Cha</span>
        </div>
    </div>
    <a href="~/Home/DeleteCharacter/@c.CharcterID"><input type="button" value="Delete Character"/></a>
}
